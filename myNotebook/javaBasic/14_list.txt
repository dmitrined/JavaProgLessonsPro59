### List (Список) в Java

`List` — это интерфейс для упорядоченных коллекций в Java. Он допускает дубликаты элементов и поддерживает доступ к элементам по их индексу (позиции).

#### 🔹 Основные методы интерфейса List:

**Добавление элементов:**
*   `add(E element)` — добавляет указанный элемент в конец списка.
*   `add(int index, E element)` — вставляет элемент по заданному индексу. Элементы после сдвигаются вправо.
*   `addAll(Collection<? extends E> c)` — добавляет все элементы из коллекции `c` в конец списка.
*   `addAll(int index, Collection<? extends E> c)` — вставляет элементы коллекции `c` начиная с индекса `index`.

**Доступ и изменение элементов:**
*   `get(int index)` — возвращает элемент по указанному индексу.
*   `set(int index, E element)` — заменяет элемент по индексу на новый. Возвращает старое значение.

**Удаление элементов:**
*   `remove(int index)` — удаляет элемент по индексу. Последующие элементы сдвигаются влево. Возвращает удаленный элемент.
*   `remove(Object o)` — удаляет первое вхождение объекта `o`. Возвращает `true`, если элемент был найден и удален.
*   `removeAll(Collection<?> c)` — удаляет все элементы, содержащиеся в коллекции `c`.
*   `retainAll(Collection<?> c)` — оставляет только те элементы, которые есть в коллекции `c`.
*   `clear()` — полностью очищает список.

**Поиск элементов:**
*   `indexOf(Object o)` — индекс первого вхождения объекта или -1.
*   `lastIndexOf(Object o)` — индекс последнего вхождения объекта или -1.
*   `contains(Object o)` — возвращает `true`, если список содержит объект.

**Работа с частью списка:**
*   `subList(int fromIndex, int toIndex)` — возвращает представление части списка от `fromIndex` (вкл.) до `toIndex` (искл.). Изменения в подсписке отражаются в оригинале.

**Информация о списке:**
*   `size()` — количество элементов.
*   `isEmpty()` — возвращает `true`, если список пуст.

**Итерация и обработка (Java 8+):**
*   `iterator()` / `listIterator()` — возвращает итератор или листитератор (для двустороннего перебора).
*   `forEach(Consumer<? super E> action)` — выполняет действие для каждого элемента.
*   `removeIf(Predicate<? super E> filter)` — удаляет элементы по условию.
*   `replaceAll(UnaryOperator<E> operator)` — заменяет каждый элемент результатом функции.
*   `sort(Comparator<? super E> c)` — сортирует элементы с использованием компаратора.

#### 🔹 Сравнение реализаций List:

| Характеристика | ArrayList | LinkedList |
| :--- | :--- | :--- |
| **Структура данных** | Динамический массив | Двусвязный список |
| **Доступ по индексу** | O(1) (быстро) | O(n) (медленно) |
| **Добавление в конец** | O(1)* | O(1) |
| **Вставка/удаление в середине** | O(n) (сдвиг) | O(n) (поиск) + O(1) |
| **Вставка/удаление в начале** | O(n) | O(1) |
| **Потребление памяти** | Эффективно | Больше (за счет ссылок) |

*\* В ArrayList добавление в конец в среднем O(1), но иногда O(n) при расширении массива.*

#### 🔹 Выбор между ArrayList и LinkedList:
*   ✅ **ArrayList** — лучший выбор для большинства сценариев, особенно если часто требуется доступ к элементам по индексу или перебор.
*   ✅ **LinkedList** — эффективен только при частых вставках и удалениях элементов в начале или конце списка (используется как `Queue` или `Deque`).
