Юнит-тестирование
Модульное тестирование, или юнит-тестирование (англ. unit testing) — процесс в программировании, позволяющий проверить на корректность отдельные модули исходного кода программы.
Идея состоит в том, чтобы писать тесты для каждой нетривиальной функции или метода. Это позволяет достаточно быстро проверить, не привело ли очередное изменение кода к регрессии, то есть к появлению ошибок в уже оттестированных местах программы, а также облегчает обнаружение и устранение таких ошибок.

Ваши тесты должны:
* Быть достоверными
* Быть независимыми (от окружения, от ранее выполненных тестов)
* Легко поддерживаться
* Легко читаться и быть простыми для понимания
* Соблюдать единую конвенцию именования
* Запускаться регулярно в автоматическом режиме

Структура теста:
1) готовим исходные данные (Arrange)
2) запускаем тестируемый метод (Act)
3) сравниваем ожидания с результатом (Assert)

Один из возможных способов именования методов такой: [Тестируемый метод]_[Сценарий]_[Ожидаемое поведение].

JUnit - один из самых популярных фреймворков, используемых для тестирования кода в Java.
* @Test - аннотация, которая "говорит", что метод тестовый
* @DisplayName - аннотация, которая позволяет описать суть теста для формирования протокола
* @Nested - аннотация, для внутреннего тестового класса

Разработка через тестирование (англ. test-driven development, TDD) — техника разработки ПО суть которой: сначала пишется тест, затем пишется код. Задача в том, что б код проходил тесты. Под конец проводится рефакторинг нового кода к соответствующим стандартам.

Пример кода (Java, JUnit):

1. Основной код (Main.java):
// Метод 1: Возвращает список четных чисел из входного списка
public static List<Integer> getEvenValues(List<Integer> list) {
    if (list == null) {
        throw new NullArgumentException("Входной список не должен быть null");
    }
    if (list.isEmpty()) {
        return List.of();
    }
    return list.stream()
            .filter(Objects::nonNull)
            .filter(i -> i % 2 == 0)
            .collect(Collectors.toList());
}

// Метод 2: Возвращает список Person, чье имя начинается с заданной буквы
public static List<Person> getNameStartAGivenLetter(List<Person> list, String letter) {
    boolean isListInvalid = list == null || list.isEmpty();
    if (isListInvalid) {
        throw new IllegalArgumentException("Список не должен быть пустым");
    }
    boolean isLetterInvalid = (letter == null) || (letter.isEmpty());
    if (isLetterInvalid) {
        throw new IllegalArgumentException("Строка буквы не должна быть пустой");
    }
    return list.stream()
            .filter(p -> p.getName().startsWith(letter))
            .collect(Collectors.toList());
}

2. Тестовый код (MainTest.java):
class MainTest {
    @Nested
    @DisplayName("Тесты для метода 'Возврат четных значений'")
    class GetEvenValues {
        @Test
        @DisplayName("Должен возвращать только четные значения")
        void getEvenValues_returnsOnlyEvenNumbers() {
            List<Integer> list = List.of(12, 9, 0, 1, 10);
            List<Integer> expected = List.of(12, 0, 10);
            List<Integer> actual = Main.getEvenValues(list);
            assertEquals(expected, actual);
        }

        @Test
        @DisplayName("Должен выбрасывать NullArgumentException для null списка")
        void getEvenValues_nullInput_throwsException() {
            NullArgumentException e = assertThrows(NullArgumentException.class, () -> Main.getEvenValues(null));
            assertEquals("Входной список не должен быть null", e.getMessage());
        }
    }
}

📌 Аннотации JUnit
Аннотация      Описание
@Test          Метод тестирования
@BeforeEach    Запускается перед каждым тестом
@AfterEach     Запускается после каждого теста
@BeforeAll     Запускается один раз перед всеми тестами
@AfterAll      Запускается один раз после всех тестов

✅ Проверки (Assertions)
JUnit использует Assertions для проверки ожидаемого результата:
Метод                               Описание
assertEquals(expected, actual)      Проверяет, равны ли значения
assertNotEquals(unexpected, actual) Проверяет, НЕ равны ли значения
assertTrue(condition)               Проверяет, истинно ли условие
assertFalse(condition)              Проверяет, ложно ли условие
assertNull(value)                   Проверяет, является ли значение null
assertNotNull(value)                Проверяет, не является ли значение null
assertThrows(exception, executable) Проверяет, выбрасывается ли ожидаемое исключение

🔎 Пример: Тестирование Calculator
public class Calculator {
    public int add(int a, int b) { return a + b; }
    public double divide(int a, int b) {
        if (b == 0) throw new ArithmeticException("Деление на ноль!");
        return (double) a / b;
    }
}

public class CalculatorTest {
    private Calculator calculator;
    @BeforeEach
    void setUp() { calculator = new Calculator(); }
    @Test
    void testAddition() { assertEquals(5, calculator.add(2, 3)); }
    @Test
    void testDivision() { assertEquals(2.5, calculator.divide(5, 2)); }
    @Test
    void testDivisionByZero() { assertThrows(ArithmeticException.class, () -> calculator.divide(5, 0)); }
}

🚀 Запуск тестов
Запустить тесты можно в IntelliJ IDEA или Eclipse через Run Tests. JUnit также поддерживает запуск через Maven:
sh
mvn test

🔥 Вывод
JUnit — мощный фреймворк для автоматизированного тестирования в Java.
✅ Позволяет проверять код на корректность.
✅ Упрощает поддержку и развитие проекта.
✅ Автоматизирует проверку ошибок.
