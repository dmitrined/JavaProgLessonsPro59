# Полное руководство по Spring Boot: Архитектура и Взаимосвязи

Это методическое пособие объясняет, как устроено типичное приложение на Spring Boot, роль каждого слоя и используемые в них аннотации.

---

## 1. Общая архитектура (Flow запроса)
Когда клиент отправляет HTTP-запрос (например, POST /cars), он проходит через следующие этапы:
1. **Controller**: Принимает запрос, извлекает данные.
2. **DTO**: Данные приходят в виде Data Transfer Object.
3. **Validation**: Проверка входящих данных.
4. **Service**: Бизнес-логика (обработка данных, вычисления).
5. **Repository**: Взаимодействие с БД через **Model (Entity)**.
6. **DB**: Сохранение данных (структура которой задается через **db.changelog**).

---

## 2. Слои приложения и их Аннотации

### 📂 Config (Конфигурация)
Используется для настройки бинов и сторонних библиотек.
*   `@Configuration`: Указывает, что класс является источником определений бинов.
*   `@Bean`: Ставится над методом внутри `@Configuration`, чтобы Spring управлял возвращаемым объектом.

### 📂 Model / Entity (Модель данных)
Отражение таблицы базы данных в Java-код.
*   `@Entity`: Указывает, что класс является JPA сущностью (связан с таблицей БД).
*   `@Table(name = "...")`: Задает имя таблицы.
*   `@Id`: Помечает поле как первичный ключ.
*   `@GeneratedValue`: Стратегия генерации ID (например, `GenerationType.IDENTITY`).
*   `@Column`: Настройка конкретной колонки (name, nullable, unique).
*   `@ManyToOne`, `@OneToMany`, `@ManyToMany`: Описание связей между таблицами.

### 📂 DTO (Data Transfer Object)
Объекты для передачи данных между клиентом и сервером. Помогают не "светить" структуру БД наружу.
*   Обычно это простые POJO или `record`.
*   Часто используются аннотации **Lombok**: `@Data`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@Builder`.

### 📂 Validation (Валидация)
Проверка данных перед их обработкой.
*   `@Valid`: Ставится в контроллере перед DTO, чтобы запустить проверку.
*   `@NotNull`, `@NotBlank`, `@Size(min=..., max=...)`, `@Min`, `@Max`: Ограничения на поля DTO.
*   `@Component`: Если создается кастомный валидатор (как `CarValidator`).

### 📂 Repository (Репозиторий)
Слой доступа к данным.
*   `@Repository`: Помечает интерфейс как репозиторий (Spring создаст реализацию автоматически).
*   Наследуется от `JpaRepository<Entity, IdType>`.
*   Позволяет использовать Query Methods (например, `findByBrand(String brand)`).

### 📂 Service (Сервис)
Здесь живет основная логика приложения.
*   `@Service`: Помечает класс как сервис.
*   `@Transactional`: Гарантирует, что набор операций в методе выполнится как одна транзакция (все или ничего).
*   Здесь происходит конвертация **DTO <-> Entity**.

### 📂 Controller (Контроллер)
Входная точка для API.
*   `@RestController`: Комбинация `@Controller` и `@ResponseBody`.
*   `@RequestMapping("/api/...")`: Базовый путь для всех методов класса.
*   `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`: Типы HTTP-запросов.
*   `@RequestBody`: Извлекает объект из тела запроса.
*   `@PathVariable`: Извлекает переменную из пути (например, `/cars/{id}`).
*   `@RequestParam`: Извлекает параметры запроса (например, `?color=red`).

---

## 3. Настройки и Инфраструктура

### 📄 application.properties / application.yml
Главный файл настроек. Здесь прописываются:
*   Подключение к БД (`spring.datasource.url`, `username`, `password`).
*   Порт сервера (`server.port`).
*   Настройки логирования (`logging.level.root`).
*   Настройки Liquibase или Hibernate.

### 📂 db.changelog (Liquibase)
Система контроля версий для базы данных.
*   Хранится в `src/main/resources/db/changelog/`.
*   Позволяет поэтапно изменять структуру БД (создавать таблицы, добавлять колонки) через XML, YAML или SQL файлы.
*   `db.changelog-master.xml`: Главный файл, который подключает остальные "чейнджсеты".

---

## 4. Как они связаны (Пример создания объекта)

1.  **Client** посылает JSON -> **Controller**.
2.  **Controller** получает **DTO**, проверяет его через `@Valid`.
3.  **Controller** вызывает **Service**.
4.  **Service** маппит (преобразует) **DTO** в **Entity**.
5.  **Service** вызывает **Repository.save(entity)**.
6.  **Repository** выполняет SQL (структура которого была создана через **db.changelog**) и сохраняет данные в **DB**.
7.  **Service** получает сохраненную сущность, маппит обратно в **DTO** и возвращает в **Controller**.
8.  **Controller** возвращает **DTO** клиенту.
